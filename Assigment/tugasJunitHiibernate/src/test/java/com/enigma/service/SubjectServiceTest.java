package com.enigma.service;

import com.enigma.model.Subject;
import org.junit.jupiter.api.Test;

import static org.junit.Assert.*;
import static org.junit.jupiter.api.Assertions.assertEquals;

/**
 * * Create at 9/26/2019 4:07 AM @author Ilhamdoanggg
 **/
public class SubjectServiceTest {

    /*@Test
    public void testEquals(){
        String subjectExpected = "Sistem Basis Data";
        String subjectAvailable = "Algoritma Pemrograman";
        Subject subject = new Subject();
        Subject subject1 = new Subject();
        if (subjectExpected.equals("Sistem Basis Data")){
            System.out.println("Subject sudah ada didalam database!");
        }else {
            SubjectService.createSubject(subjectAvailable,2);
        }
        assertNull(subjectExpected,subject);
        assertEquals(subject,subject1);
    }
*/
}